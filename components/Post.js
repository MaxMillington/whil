import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const Post = (props) => {
  return (
    <View style={styles.buttonContainer}>
      <Text>GAHOIHEOISHER</Text>
    </View>
  )
}


export default Post

const styles = StyleSheet.create({
  buttonContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  }
});
